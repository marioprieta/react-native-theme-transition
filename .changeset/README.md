# Changesets

This folder is used by [changesets](https://github.com/changesets/changesets) to track version bumps and generate changelogs.

Run `npx changeset` to create a new changeset when making changes.
