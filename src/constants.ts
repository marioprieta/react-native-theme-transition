/** Log/error prefix shared across the package. @internal */
export const TAG = '[react-native-theme-transition]'

/** Absolute fill style for overlays. @internal */
export const ABSOLUTE_FILL = { position: 'absolute', top: 0, left: 0, right: 0, bottom: 0 } as const
